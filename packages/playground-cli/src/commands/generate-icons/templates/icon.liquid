import { IconProps } from "./types";

export function {{ iconName }}({ size = {{ defaultIconSize }}, color = "currentColor", className }: IconProps) {
  return (
    <svg {{ props }} height={size} width={size} className={className}>
      {{ iconContent }}
    </svg>
  );
}
