---
category: Components
description: A component to display a table
icon: TableIcon
status: in-progress
---

`Table` is a component to display data in a table. It supports sorting and custom rendering.

## Examples

### Simple Table

<Example name="Table/SimpleTable" />

### Custom Rendering

You can customize the rendering of the table cells and headers by providing a `render` or `renderHeader` function to the column.

<Example name="Table/CustomRendering" />

<Features features={[
  'Sorting is supported by default.',
  'Custom rendering of cells and headers.',
]} />

## API

### Table Props

<Properties properties={[
  { name: 'columns', type: 'TableColumn<T>[]', description: 'The columns to display in the table.', required: true },
  { name: 'data', type: 'T[]', description: 'The data to display in the table.', required: true },
  { name: 'maxHeight', type: 'string | number', description: 'The maximum height of the table.' },
]} />

### TableColumn Type

```typescript
type TableColumn<T extends Record<string, any>> = {
  key: keyof T;
  label: string;
  render?: (value: T[keyof T]) => React.ReactNode;
  renderHeader?: (label: string) => React.ReactNode;
  sortable?: boolean;
  sortFn?: (a: T, b: T, order: "asc" | "desc" | null) => number;
  sort?: "asc" | "desc" | null;
  width?: string | number;
};
```