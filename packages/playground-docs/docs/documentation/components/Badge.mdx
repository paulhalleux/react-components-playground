---
category: Components
description: A component to display a badge
icon: BadgeIcon
status: done
sourceUrl: https://github.com/paulhalleux/react-playground/blob/main/packages/playground/src/components/Badge/Badge.tsx
---

`Badge` is a component to display a badge. It can be used to display a status, a count, or any other information.

## Examples

<Example name="Badge" />

## API

### \{:component} Badge

#### Usage

```tsx
<Badge variant="primary" size="small">Badge</Badge>
```

#### Properties

<Properties component="Badge" />

### \{:type} BadgeSize

The `BadgeSize` type is an enum with the following values:

```ts
type BadgeSize = "small" | "medium" | "large"
```

### \{:type} BadgeVariant

The `BadgeVariant` type is an enum with the following values:

```ts
type BadgeVariant = "primary" | "secondary" | "default" | "danger" | "success" | "info" | "ghost"
```